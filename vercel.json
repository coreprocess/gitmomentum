{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm build",
  "installCommand": "git fetch --unshallow --tags --force 2>/dev/null || true; git fetch --tags --force 2>/dev/null || true; pnpm install --frozen-lockfile",
  "outputDirectory": "dist"
}
